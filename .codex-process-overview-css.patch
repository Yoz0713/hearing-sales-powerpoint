*** Begin Patch
*** Update File: src/styles/process-overview.css
@@
-.process-overview__path {
-  position: relative;
+.process-overview__journey {
   min-width: 0;
   min-height: 0;
   display: grid;
+  grid-template-rows: 184px minmax(0, 1fr);
+  gap: 18px;
+  overflow: hidden;
+}
+
+.process-overview__workflow {
+  position: relative;
+  min-width: 0;
+  min-height: 0;
+  margin: 0;
+  padding: 0;
+  display: grid;
   grid-template-columns: repeat(10, minmax(0, 1fr));
   grid-auto-flow: dense;
+  list-style: none;
   overflow: hidden;
-  box-shadow: 0 18px 44px rgba(48, 72, 59, 0.12);
+  border-top: 1px solid rgba(69, 95, 81, 0.2);
+  border-bottom: 1px solid rgba(69, 95, 81, 0.2);
+  background: rgba(255, 255, 255, 0.34);
 }
 
-/* 中性底線 + 兩個橘色刻點，標出 4／3／3 的分組——顏色只留一種，仍看得出「切成三段」。 */
 .process-overview__track {
   position: absolute;
-  z-index: 4;
-  left: 0;
-  right: 0;
-  bottom: 117px;
-  height: 6px;
-  background: linear-gradient(
-    90deg,
-    rgba(69, 95, 81, 0.22) 0 39%,
-    var(--color-orange) 39% 41%,
-    rgba(69, 95, 81, 0.22) 41% 69%,
-    var(--color-orange) 69% 71%,
-    rgba(69, 95, 81, 0.22) 71% 100%
-  );
+  z-index: 1;
+  left: 3%;
+  right: 3%;
+  top: 53px;
+  height: 2px;
+  background: rgba(69, 95, 81, 0.28);
   pointer-events: none;
 }
 
-/* 三張卡片統一中性底色，不再用背景色分類——靠編號、留白與 4/3/3 的步驟格數辨識彼此。 */
-.process-overview__phase {
+.process-overview__workflow-step {
   position: relative;
-  z-index: 1;
+  z-index: 2;
   box-sizing: border-box;
   min-width: 0;
   min-height: 0;
-  padding: 34px 32px 24px;
+  padding: 30px 12px 22px;
   display: flex;
   flex-direction: column;
+  align-items: center;
+  gap: 28px;
   overflow: hidden;
-  color: var(--color-text-heading);
+  text-align: center;
 }
 
-.process-overview__phase--span-4 {
-  grid-column: span 4;
+.process-overview__workflow-step + .process-overview__workflow-step {
+  border-left: 1px solid rgba(69, 95, 81, 0.14);
 }
 
-.process-overview__phase--span-3 {
-  grid-column: span 3;
+.process-overview__workflow-step::after {
+  position: absolute;
+  top: 45px;
+  left: 50%;
+  width: 16px;
+  height: 16px;
+  border: 4px solid var(--color-surface-base);
+  border-radius: 50%;
+  background: var(--color-orange);
+  content: '';
+  transform: translateX(-50%);
 }
 
-.process-overview__phase + .process-overview__phase {
-  border-left: 1px solid rgba(69, 95, 81, 0.2);
+.process-overview__workflow-step > span {
+  color: rgba(69, 95, 81, 0.58);
+  font-family: var(--font-family-latin);
+  font-size: 20px;
+  font-weight: 700;
+  line-height: 1;
+  letter-spacing: 0.08em;
+}
+
+.process-overview__workflow-step > strong {
+  max-width: 142px;
+  color: var(--color-text-heading);
+  font-family: var(--font-family-base);
+  font-size: 24px;
+  font-weight: var(--font-weight-bold);
+  line-height: 1.24;
+  text-wrap: balance;
+}
+
+.process-overview__phases {
+  min-width: 0;
+  min-height: 0;
+  display: grid;
+  grid-template-columns: repeat(10, minmax(0, 1fr));
+  grid-auto-flow: dense;
+  overflow: hidden;
+  background: rgba(48, 72, 59, 0.08);
+}
+
+.process-overview__phase {
+  position: relative;
+  box-sizing: border-box;
+  min-width: 0;
+  min-height: 0;
+  padding: 28px 30px;
+  display: grid;
+  grid-template-columns: 48px minmax(0, 1fr);
+  align-items: start;
+  gap: 18px;
+  overflow: hidden;
+  color: var(--color-text-on-brand);
+  background: var(--color-forest-900);
+  clip-path: inset(18% 4% 0 4%);
+  transform: translateY(34px) scale(0.96);
+  transition:
+    opacity 0.46s ease,
+    clip-path 0.62s cubic-bezier(0.2, 0.8, 0.2, 1),
+    transform 0.62s cubic-bezier(0.2, 0.8, 0.2, 1);
+}
+
+.process-overview__phase.visible {
+  clip-path: inset(0);
+  transform: none;
+}
+
+.process-overview__phase--span-4 {
+  grid-column: span 4;
+}
+
+.process-overview__phase--span-3 {
+  grid-column: span 3;
+}
+
+.process-overview__phase + .process-overview__phase {
+  border-left: 1px solid rgba(255, 255, 255, 0.22);
 }
 
 .process-overview__phase-index {
-  color: var(--color-orange);
+  padding-top: 4px;
+  color: var(--color-brand-primary-bright);
   font-family: var(--font-family-latin);
   font-size: 20px;
@@
 .process-overview .process-overview__phase blockquote {
-  margin: 10px 0 0;
-  color: rgba(69, 95, 81, 0.7);
+  margin: 0;
+  color: rgba(255, 255, 255, 0.7);
   font-family: var(--font-family-base);
   font-size: 24px;
@@
 .process-overview .process-overview__phase h2 {
   max-width: 440px;
-  margin: 16px 0 0;
+  margin: 18px 0 0;
   color: inherit;
   font-family: var(--font-family-display);
-  font-size: 38px;
+  font-size: 36px;
@@
-.process-overview__steps {
-  position: relative;
-  z-index: 5;
-  min-width: 0;
-  margin: auto 0 0;
-  padding: 34px 0 0;
-  list-style: none;
-  display: grid;
-  grid-auto-flow: dense;
-  transform: translateY(20px);
-  transition: opacity 0.46s ease, transform 0.46s cubic-bezier(0.2, 0.8, 0.2, 1);
-}
-
-.process-overview__steps.visible {
-  transform: none;
-}
-
-.process-overview__steps li {
-  position: relative;
-  min-width: 0;
-  padding: 18px 12px 0;
-  text-align: center;
-}
-
-.process-overview__steps li::before {
-  position: absolute;
-  top: -7px;
-  left: 50%;
-  width: 14px;
-  height: 14px;
-  border: 4px solid var(--color-surface-base);
-  border-radius: 50%;
-  background: currentColor;
-  content: '';
-  transform: translateX(-50%);
-}
-
-.process-overview__steps li + li {
-  border-left: 1px solid rgba(69, 95, 81, 0.17);
-}
-
-.process-overview__steps span {
-  display: block;
-  font-size: 24px;
-  font-weight: var(--font-weight-bold);
-  line-height: 1.25;
-  white-space: nowrap;
-}
-
 .process-overview__handoff {
@@
 @media (prefers-reduced-motion: reduce) {
-  .process-overview__steps,
+  .process-overview__phase,
   .process-overview__handoff {
*** End Patch
